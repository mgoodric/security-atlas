# pkg/

Public Go packages (SDKs and other consumer-facing libraries). Anything here is importable by external consumers.

| Package | Purpose | First slice |
|---|---|---|
| `pkg/sdk-go/` | Go SDK for the evidence push API (`client.evidence.Push`, etc.) | 003 |

Empty in slice 001.
