// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: csf_assessment.sql

package dbx

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteCsfProfileSelection = `-- name: DeleteCsfProfileSelection :one
DELETE FROM csf_profile_selections
WHERE tenant_id = $1 AND csf_profile_id = $2 AND framework_requirement_id = $3
RETURNING id, tenant_id, csf_profile_id, framework_requirement_id, target_outcome, note, created_at, updated_at
`

type DeleteCsfProfileSelectionParams struct {
	TenantID               pgtype.UUID `json:"tenant_id"`
	CsfProfileID           pgtype.UUID `json:"csf_profile_id"`
	FrameworkRequirementID pgtype.UUID `json:"framework_requirement_id"`
}

// Clear a Subcategory selection (the operator removed the target outcome).
// Returns the deleted row so the caller can audit what was cleared.
func (q *Queries) DeleteCsfProfileSelection(ctx context.Context, arg DeleteCsfProfileSelectionParams) (CsfProfileSelection, error) {
	row := q.db.QueryRow(ctx, deleteCsfProfileSelection, arg.TenantID, arg.CsfProfileID, arg.FrameworkRequirementID)
	var i CsfProfileSelection
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.CsfProfileID,
		&i.FrameworkRequirementID,
		&i.TargetOutcome,
		&i.Note,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getCsfProfile = `-- name: GetCsfProfile :one
SELECT id, tenant_id, framework_version_id, kind, name, created_by, created_at, updated_at
FROM csf_profiles
WHERE tenant_id = $1 AND framework_version_id = $2 AND kind = $3
`

type GetCsfProfileParams struct {
	TenantID           pgtype.UUID    `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID    `json:"framework_version_id"`
	Kind               CsfProfileKind `json:"kind"`
}

func (q *Queries) GetCsfProfile(ctx context.Context, arg GetCsfProfileParams) (CsfProfile, error) {
	row := q.db.QueryRow(ctx, getCsfProfile, arg.TenantID, arg.FrameworkVersionID, arg.Kind)
	var i CsfProfile
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.FrameworkVersionID,
		&i.Kind,
		&i.Name,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getCsfProfileByID = `-- name: GetCsfProfileByID :one
SELECT id, tenant_id, framework_version_id, kind, name, created_by, created_at, updated_at
FROM csf_profiles
WHERE tenant_id = $1 AND id = $2
`

type GetCsfProfileByIDParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	ID       pgtype.UUID `json:"id"`
}

func (q *Queries) GetCsfProfileByID(ctx context.Context, arg GetCsfProfileByIDParams) (CsfProfile, error) {
	row := q.db.QueryRow(ctx, getCsfProfileByID, arg.TenantID, arg.ID)
	var i CsfProfile
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.FrameworkVersionID,
		&i.Kind,
		&i.Name,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getCsfTierRating = `-- name: GetCsfTierRating :one
SELECT id, tenant_id, framework_version_id, tier, rationale, rated_by, rated_at, created_at, updated_at
FROM csf_tier_ratings
WHERE tenant_id = $1 AND framework_version_id = $2
`

type GetCsfTierRatingParams struct {
	TenantID           pgtype.UUID `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID `json:"framework_version_id"`
}

func (q *Queries) GetCsfTierRating(ctx context.Context, arg GetCsfTierRatingParams) (CsfTierRating, error) {
	row := q.db.QueryRow(ctx, getCsfTierRating, arg.TenantID, arg.FrameworkVersionID)
	var i CsfTierRating
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.FrameworkVersionID,
		&i.Tier,
		&i.Rationale,
		&i.RatedBy,
		&i.RatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertCsfAssessmentAudit = `-- name: InsertCsfAssessmentAudit :one

INSERT INTO csf_assessment_audit (
    id, tenant_id, framework_version_id, subject_kind, subject_id, action, actor, detail
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, tenant_id, framework_version_id, subject_kind, subject_id, action, actor, detail, occurred_at
`

type InsertCsfAssessmentAuditParams struct {
	ID                 pgtype.UUID `json:"id"`
	TenantID           pgtype.UUID `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID `json:"framework_version_id"`
	SubjectKind        string      `json:"subject_kind"`
	SubjectID          pgtype.UUID `json:"subject_id"`
	Action             string      `json:"action"`
	Actor              string      `json:"actor"`
	Detail             string      `json:"detail"`
}

// ===== csf_assessment_audit =====
func (q *Queries) InsertCsfAssessmentAudit(ctx context.Context, arg InsertCsfAssessmentAuditParams) (CsfAssessmentAudit, error) {
	row := q.db.QueryRow(ctx, insertCsfAssessmentAudit,
		arg.ID,
		arg.TenantID,
		arg.FrameworkVersionID,
		arg.SubjectKind,
		arg.SubjectID,
		arg.Action,
		arg.Actor,
		arg.Detail,
	)
	var i CsfAssessmentAudit
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.FrameworkVersionID,
		&i.SubjectKind,
		&i.SubjectID,
		&i.Action,
		&i.Actor,
		&i.Detail,
		&i.OccurredAt,
	)
	return i, err
}

const listCsfAssessmentAudit = `-- name: ListCsfAssessmentAudit :many
SELECT id, tenant_id, framework_version_id, subject_kind, subject_id, action, actor, detail, occurred_at
FROM csf_assessment_audit
WHERE tenant_id = $1 AND framework_version_id = $2
ORDER BY occurred_at DESC, id ASC
`

type ListCsfAssessmentAuditParams struct {
	TenantID           pgtype.UUID `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID `json:"framework_version_id"`
}

// Newest first. Exposed for the audit trail + integration assertions.
func (q *Queries) ListCsfAssessmentAudit(ctx context.Context, arg ListCsfAssessmentAuditParams) ([]CsfAssessmentAudit, error) {
	rows, err := q.db.Query(ctx, listCsfAssessmentAudit, arg.TenantID, arg.FrameworkVersionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CsfAssessmentAudit
	for rows.Next() {
		var i CsfAssessmentAudit
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.FrameworkVersionID,
			&i.SubjectKind,
			&i.SubjectID,
			&i.Action,
			&i.Actor,
			&i.Detail,
			&i.OccurredAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listCsfProfileSelectionsWithSubcategory = `-- name: ListCsfProfileSelectionsWithSubcategory :many
SELECT
    s.id,
    s.csf_profile_id,
    s.framework_requirement_id,
    s.target_outcome,
    s.note,
    fr.code  AS subcategory_code,
    fr.title AS subcategory_title
FROM csf_profile_selections s
JOIN framework_requirements fr ON fr.id = s.framework_requirement_id
WHERE s.tenant_id = $1 AND s.csf_profile_id = $2
ORDER BY fr.code
`

type ListCsfProfileSelectionsWithSubcategoryParams struct {
	TenantID     pgtype.UUID `json:"tenant_id"`
	CsfProfileID pgtype.UUID `json:"csf_profile_id"`
}

type ListCsfProfileSelectionsWithSubcategoryRow struct {
	ID                     pgtype.UUID `json:"id"`
	CsfProfileID           pgtype.UUID `json:"csf_profile_id"`
	FrameworkRequirementID pgtype.UUID `json:"framework_requirement_id"`
	TargetOutcome          string      `json:"target_outcome"`
	Note                   string      `json:"note"`
	SubcategoryCode        string      `json:"subcategory_code"`
	SubcategoryTitle       string      `json:"subcategory_title"`
}

// The gap-view row source for ONE profile: every selection joined to its
// shared CSF Subcategory (code + title) so the handler can render the
// per-Subcategory target outcome without a second round trip. Newest CSF
// Subcategory ordering is by code (e.g. GV.OC-01, GV.OC-02, …).
func (q *Queries) ListCsfProfileSelectionsWithSubcategory(ctx context.Context, arg ListCsfProfileSelectionsWithSubcategoryParams) ([]ListCsfProfileSelectionsWithSubcategoryRow, error) {
	rows, err := q.db.Query(ctx, listCsfProfileSelectionsWithSubcategory, arg.TenantID, arg.CsfProfileID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListCsfProfileSelectionsWithSubcategoryRow
	for rows.Next() {
		var i ListCsfProfileSelectionsWithSubcategoryRow
		if err := rows.Scan(
			&i.ID,
			&i.CsfProfileID,
			&i.FrameworkRequirementID,
			&i.TargetOutcome,
			&i.Note,
			&i.SubcategoryCode,
			&i.SubcategoryTitle,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertCsfProfile = `-- name: UpsertCsfProfile :one

INSERT INTO csf_profiles (
    id, tenant_id, framework_version_id, kind, name, created_by
)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (tenant_id, framework_version_id, kind) DO UPDATE
SET name       = EXCLUDED.name,
    updated_at = now()
RETURNING id, tenant_id, framework_version_id, kind, name, created_by, created_at, updated_at, (xmax = 0) AS inserted
`

type UpsertCsfProfileParams struct {
	ID                 pgtype.UUID    `json:"id"`
	TenantID           pgtype.UUID    `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID    `json:"framework_version_id"`
	Kind               CsfProfileKind `json:"kind"`
	Name               string         `json:"name"`
	CreatedBy          string         `json:"created_by"`
}

type UpsertCsfProfileRow struct {
	ID                 pgtype.UUID        `json:"id"`
	TenantID           pgtype.UUID        `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID        `json:"framework_version_id"`
	Kind               CsfProfileKind     `json:"kind"`
	Name               string             `json:"name"`
	CreatedBy          string             `json:"created_by"`
	CreatedAt          pgtype.Timestamptz `json:"created_at"`
	UpdatedAt          pgtype.Timestamptz `json:"updated_at"`
	Inserted           bool               `json:"inserted"`
}

// ===== csf_profiles =====
// Insert (or no-op update) the single profile per (tenant, framework_version,
// kind). Re-creating the same kind returns the existing row (its name/updated
// refreshed) rather than erroring, so the editor is idempotent.
func (q *Queries) UpsertCsfProfile(ctx context.Context, arg UpsertCsfProfileParams) (UpsertCsfProfileRow, error) {
	row := q.db.QueryRow(ctx, upsertCsfProfile,
		arg.ID,
		arg.TenantID,
		arg.FrameworkVersionID,
		arg.Kind,
		arg.Name,
		arg.CreatedBy,
	)
	var i UpsertCsfProfileRow
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.FrameworkVersionID,
		&i.Kind,
		&i.Name,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Inserted,
	)
	return i, err
}

const upsertCsfProfileSelection = `-- name: UpsertCsfProfileSelection :one

INSERT INTO csf_profile_selections (
    id, tenant_id, csf_profile_id, framework_requirement_id, target_outcome, note
)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (csf_profile_id, framework_requirement_id) DO UPDATE
SET target_outcome = EXCLUDED.target_outcome,
    note           = EXCLUDED.note,
    updated_at     = now()
RETURNING id, tenant_id, csf_profile_id, framework_requirement_id, target_outcome, note, created_at, updated_at, (xmax = 0) AS inserted
`

type UpsertCsfProfileSelectionParams struct {
	ID                     pgtype.UUID `json:"id"`
	TenantID               pgtype.UUID `json:"tenant_id"`
	CsfProfileID           pgtype.UUID `json:"csf_profile_id"`
	FrameworkRequirementID pgtype.UUID `json:"framework_requirement_id"`
	TargetOutcome          string      `json:"target_outcome"`
	Note                   string      `json:"note"`
}

type UpsertCsfProfileSelectionRow struct {
	ID                     pgtype.UUID        `json:"id"`
	TenantID               pgtype.UUID        `json:"tenant_id"`
	CsfProfileID           pgtype.UUID        `json:"csf_profile_id"`
	FrameworkRequirementID pgtype.UUID        `json:"framework_requirement_id"`
	TargetOutcome          string             `json:"target_outcome"`
	Note                   string             `json:"note"`
	CreatedAt              pgtype.Timestamptz `json:"created_at"`
	UpdatedAt              pgtype.Timestamptz `json:"updated_at"`
	Inserted               bool               `json:"inserted"`
}

// ===== csf_profile_selections =====
// Set the target outcome for one Subcategory inside a profile. Re-setting the
// same (profile, Subcategory) updates the outcome + note in place.
func (q *Queries) UpsertCsfProfileSelection(ctx context.Context, arg UpsertCsfProfileSelectionParams) (UpsertCsfProfileSelectionRow, error) {
	row := q.db.QueryRow(ctx, upsertCsfProfileSelection,
		arg.ID,
		arg.TenantID,
		arg.CsfProfileID,
		arg.FrameworkRequirementID,
		arg.TargetOutcome,
		arg.Note,
	)
	var i UpsertCsfProfileSelectionRow
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.CsfProfileID,
		&i.FrameworkRequirementID,
		&i.TargetOutcome,
		&i.Note,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Inserted,
	)
	return i, err
}

const upsertCsfTierRating = `-- name: UpsertCsfTierRating :one


INSERT INTO csf_tier_ratings (
    id, tenant_id, framework_version_id, tier, rationale, rated_by
)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (tenant_id, framework_version_id) DO UPDATE
SET tier       = EXCLUDED.tier,
    rationale  = EXCLUDED.rationale,
    rated_by   = EXCLUDED.rated_by,
    rated_at   = now(),
    updated_at = now()
RETURNING id, tenant_id, framework_version_id, tier, rationale, rated_by, rated_at, created_at, updated_at, (xmax = 0) AS inserted
`

type UpsertCsfTierRatingParams struct {
	ID                 pgtype.UUID `json:"id"`
	TenantID           pgtype.UUID `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID `json:"framework_version_id"`
	Tier               CsfTier     `json:"tier"`
	Rationale          string      `json:"rationale"`
	RatedBy            string      `json:"rated_by"`
}

type UpsertCsfTierRatingRow struct {
	ID                 pgtype.UUID        `json:"id"`
	TenantID           pgtype.UUID        `json:"tenant_id"`
	FrameworkVersionID pgtype.UUID        `json:"framework_version_id"`
	Tier               CsfTier            `json:"tier"`
	Rationale          string             `json:"rationale"`
	RatedBy            string             `json:"rated_by"`
	RatedAt            pgtype.Timestamptz `json:"rated_at"`
	CreatedAt          pgtype.Timestamptz `json:"created_at"`
	UpdatedAt          pgtype.Timestamptz `json:"updated_at"`
	Inserted           bool               `json:"inserted"`
}

// Slice 515 — NIST CSF 2.0 Tier / Profile assessment queries.
//
// Conventions match slice 018 / 512:
//   - tenant_id is always the first parameter on tenant-scoped reads even
//     though RLS reads it from the app.current_tenant GUC — the explicit
//     predicate keeps query plans on the (tenant_id, …) indexes.
//   - Time stamps (now()) are issued by the DB so the application doesn't
//     choose a clock.
//   - The gap-view read joins selections to the SHARED CSF Subcategory rows
//     (framework_requirements) by FK — the per-Subcategory↔SCF-anchor mapping
//     is NEVER re-stored here (invariant #1, P0-515-2); the anchor/coverage
//     traversal is done by internal/api/ucfcoverage at read time.
//
// ===== csf_tier_ratings =====
// Insert or update the single Tier rating per (tenant, framework_version).
// Re-rating updates the row in place; the caller appends a csf_assessment_audit
// row ('tier_rated' on insert, 'tier_rerated' on the conflict path) and reads
// the returned `(xmax = 0)` flag to know which it was.
func (q *Queries) UpsertCsfTierRating(ctx context.Context, arg UpsertCsfTierRatingParams) (UpsertCsfTierRatingRow, error) {
	row := q.db.QueryRow(ctx, upsertCsfTierRating,
		arg.ID,
		arg.TenantID,
		arg.FrameworkVersionID,
		arg.Tier,
		arg.Rationale,
		arg.RatedBy,
	)
	var i UpsertCsfTierRatingRow
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.FrameworkVersionID,
		&i.Tier,
		&i.Rationale,
		&i.RatedBy,
		&i.RatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Inserted,
	)
	return i, err
}
