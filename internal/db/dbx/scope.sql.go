// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: scope.sql

package dbx

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countScopeCells = `-- name: CountScopeCells :one
SELECT COUNT(*)::bigint
FROM scope_cells
WHERE tenant_id = $1
`

func (q *Queries) CountScopeCells(ctx context.Context, tenantID pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countScopeCells, tenantID)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const createScopeCell = `-- name: CreateScopeCell :one
INSERT INTO scope_cells (id, tenant_id, label, dimensions, dimensions_hash)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, tenant_id, label, dimensions, dimensions_hash, created_at, updated_at
`

type CreateScopeCellParams struct {
	ID             pgtype.UUID `json:"id"`
	TenantID       pgtype.UUID `json:"tenant_id"`
	Label          string      `json:"label"`
	Dimensions     []byte      `json:"dimensions"`
	DimensionsHash string      `json:"dimensions_hash"`
}

// Insert a scope cell. dimensions_hash is the application-computed canonical
// hash; the UNIQUE (tenant_id, dimensions_hash) constraint rejects duplicates.
func (q *Queries) CreateScopeCell(ctx context.Context, arg CreateScopeCellParams) (ScopeCell, error) {
	row := q.db.QueryRow(ctx, createScopeCell,
		arg.ID,
		arg.TenantID,
		arg.Label,
		arg.Dimensions,
		arg.DimensionsHash,
	)
	var i ScopeCell
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Label,
		&i.Dimensions,
		&i.DimensionsHash,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createScopeDimension = `-- name: CreateScopeDimension :one
INSERT INTO scope_dimensions (id, tenant_id, name, value_type, allowed_values, is_required, is_builtin)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, tenant_id, name, value_type, allowed_values, is_required, is_builtin, created_at, updated_at
`

type CreateScopeDimensionParams struct {
	ID            pgtype.UUID `json:"id"`
	TenantID      pgtype.UUID `json:"tenant_id"`
	Name          string      `json:"name"`
	ValueType     string      `json:"value_type"`
	AllowedValues []byte      `json:"allowed_values"`
	IsRequired    bool        `json:"is_required"`
	IsBuiltin     bool        `json:"is_builtin"`
}

// Insert a per-tenant scope dimension declaration. Used by the bootstrap seed
// (is_builtin=true) and by admins adding custom dimensions (is_builtin=false).
// tenant_id is captured directly so RLS evaluates the policy on insert.
func (q *Queries) CreateScopeDimension(ctx context.Context, arg CreateScopeDimensionParams) (ScopeDimension, error) {
	row := q.db.QueryRow(ctx, createScopeDimension,
		arg.ID,
		arg.TenantID,
		arg.Name,
		arg.ValueType,
		arg.AllowedValues,
		arg.IsRequired,
		arg.IsBuiltin,
	)
	var i ScopeDimension
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.ValueType,
		&i.AllowedValues,
		&i.IsRequired,
		&i.IsBuiltin,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getControlApplicabilityExpr = `-- name: GetControlApplicabilityExpr :one
SELECT id, applicability_expr
FROM controls
WHERE tenant_id = $1 AND id = $2
`

type GetControlApplicabilityExprParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	ID       pgtype.UUID `json:"id"`
}

type GetControlApplicabilityExprRow struct {
	ID                pgtype.UUID `json:"id"`
	ApplicabilityExpr string      `json:"applicability_expr"`
}

// Returns the JSON-encoded applicability_expr for a single control. The column
// is TEXT (slice 002); slice 017 stores JSON in that text.
func (q *Queries) GetControlApplicabilityExpr(ctx context.Context, arg GetControlApplicabilityExprParams) (GetControlApplicabilityExprRow, error) {
	row := q.db.QueryRow(ctx, getControlApplicabilityExpr, arg.TenantID, arg.ID)
	var i GetControlApplicabilityExprRow
	err := row.Scan(&i.ID, &i.ApplicabilityExpr)
	return i, err
}

const getScopeCellByHash = `-- name: GetScopeCellByHash :one
SELECT id, tenant_id, label, dimensions, dimensions_hash, created_at, updated_at
FROM scope_cells
WHERE tenant_id = $1 AND dimensions_hash = $2
`

type GetScopeCellByHashParams struct {
	TenantID       pgtype.UUID `json:"tenant_id"`
	DimensionsHash string      `json:"dimensions_hash"`
}

// Look up a cell by its dimensions hash. Used by the "create or get" path so
// a re-seed call does not 409 on the existing default cell.
func (q *Queries) GetScopeCellByHash(ctx context.Context, arg GetScopeCellByHashParams) (ScopeCell, error) {
	row := q.db.QueryRow(ctx, getScopeCellByHash, arg.TenantID, arg.DimensionsHash)
	var i ScopeCell
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Label,
		&i.Dimensions,
		&i.DimensionsHash,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getScopeCellByID = `-- name: GetScopeCellByID :one
SELECT id, tenant_id, label, dimensions, dimensions_hash, created_at, updated_at
FROM scope_cells
WHERE tenant_id = $1 AND id = $2
`

type GetScopeCellByIDParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	ID       pgtype.UUID `json:"id"`
}

func (q *Queries) GetScopeCellByID(ctx context.Context, arg GetScopeCellByIDParams) (ScopeCell, error) {
	row := q.db.QueryRow(ctx, getScopeCellByID, arg.TenantID, arg.ID)
	var i ScopeCell
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Label,
		&i.Dimensions,
		&i.DimensionsHash,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getScopeDimensionByName = `-- name: GetScopeDimensionByName :one
SELECT id, tenant_id, name, value_type, allowed_values, is_required, is_builtin, created_at, updated_at
FROM scope_dimensions
WHERE tenant_id = $1 AND name = $2
`

type GetScopeDimensionByNameParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	Name     string      `json:"name"`
}

// Resolve a dimension by name within the current tenant context.
func (q *Queries) GetScopeDimensionByName(ctx context.Context, arg GetScopeDimensionByNameParams) (ScopeDimension, error) {
	row := q.db.QueryRow(ctx, getScopeDimensionByName, arg.TenantID, arg.Name)
	var i ScopeDimension
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.ValueType,
		&i.AllowedValues,
		&i.IsRequired,
		&i.IsBuiltin,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listScopeCells = `-- name: ListScopeCells :many
SELECT id, tenant_id, label, dimensions, dimensions_hash, created_at, updated_at
FROM scope_cells
WHERE tenant_id = $1
ORDER BY created_at DESC, id ASC
`

// Enumerate the active tenant's scope cells. Newest first.
func (q *Queries) ListScopeCells(ctx context.Context, tenantID pgtype.UUID) ([]ScopeCell, error) {
	rows, err := q.db.Query(ctx, listScopeCells, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ScopeCell
	for rows.Next() {
		var i ScopeCell
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Label,
			&i.Dimensions,
			&i.DimensionsHash,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listScopeDimensions = `-- name: ListScopeDimensions :many
SELECT id, tenant_id, name, value_type, allowed_values, is_required, is_builtin, created_at, updated_at
FROM scope_dimensions
WHERE tenant_id = $1
ORDER BY is_builtin DESC, name ASC
`

// Enumerate the active tenant's declared dimensions. Ordering: builtins first
// (stable presentation in the admin UI), then alphabetical by name.
func (q *Queries) ListScopeDimensions(ctx context.Context, tenantID pgtype.UUID) ([]ScopeDimension, error) {
	rows, err := q.db.Query(ctx, listScopeDimensions, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ScopeDimension
	for rows.Next() {
		var i ScopeDimension
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.ValueType,
			&i.AllowedValues,
			&i.IsRequired,
			&i.IsBuiltin,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
