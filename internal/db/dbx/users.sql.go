// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: users.sql

package dbx

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createUser = `-- name: CreateUser :one
INSERT INTO users (
    id, tenant_id, email, display_name, status, idp_issuer, idp_subject
)
VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
RETURNING id, tenant_id, email, display_name, status, idp_issuer, idp_subject, created_at, updated_at
`

type CreateUserParams struct {
	ID          pgtype.UUID `json:"id"`
	TenantID    pgtype.UUID `json:"tenant_id"`
	Email       string      `json:"email"`
	DisplayName string      `json:"display_name"`
	Status      string      `json:"status"`
	IdpIssuer   string      `json:"idp_issuer"`
	IdpSubject  string      `json:"idp_subject"`
}

// Insert a new user (local-mode). For OIDC-provisioned users use UpsertUserByIdpSubject
// which also handles tenant_id, display_name, and the idp_* fields.
func (q *Queries) CreateUser(ctx context.Context, arg CreateUserParams) (User, error) {
	row := q.db.QueryRow(ctx, createUser,
		arg.ID,
		arg.TenantID,
		arg.Email,
		arg.DisplayName,
		arg.Status,
		arg.IdpIssuer,
		arg.IdpSubject,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Email,
		&i.DisplayName,
		&i.Status,
		&i.IdpIssuer,
		&i.IdpSubject,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getUserByEmail = `-- name: GetUserByEmail :one
SELECT id, tenant_id, email, display_name, status, idp_issuer, idp_subject, created_at, updated_at
FROM users
WHERE tenant_id = $1 AND lower(email) = lower($2::text)
`

type GetUserByEmailParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	Email    string      `json:"email"`
}

// Lookup by case-insensitive email within a tenant. Used by /auth/local/login.
func (q *Queries) GetUserByEmail(ctx context.Context, arg GetUserByEmailParams) (User, error) {
	row := q.db.QueryRow(ctx, getUserByEmail, arg.TenantID, arg.Email)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Email,
		&i.DisplayName,
		&i.Status,
		&i.IdpIssuer,
		&i.IdpSubject,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getUserByID = `-- name: GetUserByID :one
SELECT id, tenant_id, email, display_name, status, idp_issuer, idp_subject, created_at, updated_at
FROM users
WHERE tenant_id = $1 AND id = $2
`

type GetUserByIDParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	ID       pgtype.UUID `json:"id"`
}

func (q *Queries) GetUserByID(ctx context.Context, arg GetUserByIDParams) (User, error) {
	row := q.db.QueryRow(ctx, getUserByID, arg.TenantID, arg.ID)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Email,
		&i.DisplayName,
		&i.Status,
		&i.IdpIssuer,
		&i.IdpSubject,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertUserByIdpSubject = `-- name: UpsertUserByIdpSubject :one
INSERT INTO users (
    id, tenant_id, email, display_name, status, idp_issuer, idp_subject
)
VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
ON CONFLICT (idp_issuer, idp_subject)
    WHERE idp_issuer <> '' AND idp_subject <> ''
DO UPDATE SET
    email = EXCLUDED.email,
    display_name = EXCLUDED.display_name,
    updated_at = now()
RETURNING id, tenant_id, email, display_name, status, idp_issuer, idp_subject, created_at, updated_at
`

type UpsertUserByIdpSubjectParams struct {
	ID          pgtype.UUID `json:"id"`
	TenantID    pgtype.UUID `json:"tenant_id"`
	Email       string      `json:"email"`
	DisplayName string      `json:"display_name"`
	Status      string      `json:"status"`
	IdpIssuer   string      `json:"idp_issuer"`
	IdpSubject  string      `json:"idp_subject"`
}

// Used by the OIDC callback: provision-on-first-sign-in by (idp_issuer, idp_subject).
// The composite UNIQUE index on (idp_issuer, idp_subject) WHERE both non-empty
// is the conflict target. On conflict we update display_name + email (the IdP
// is canonical) and updated_at.
func (q *Queries) UpsertUserByIdpSubject(ctx context.Context, arg UpsertUserByIdpSubjectParams) (User, error) {
	row := q.db.QueryRow(ctx, upsertUserByIdpSubject,
		arg.ID,
		arg.TenantID,
		arg.Email,
		arg.DisplayName,
		arg.Status,
		arg.IdpIssuer,
		arg.IdpSubject,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Email,
		&i.DisplayName,
		&i.Status,
		&i.IdpIssuer,
		&i.IdpSubject,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
