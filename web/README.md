# web/

Next.js 15 App Router frontend (shadcn/ui + Tailwind 4 + TanStack Query).

Real Next.js scaffold lands in slice 005 (frontend bootstrap). For slice 001, this directory contains a minimal `package.json` so the npm workspace at the repo root recognizes it.

See visual references at `Plans/mockups/` (HTML + Tailwind via CDN, iteration-1 only — these graduate to React components in slice 005).
