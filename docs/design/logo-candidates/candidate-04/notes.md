# Candidate 04 — Lattice mesh A

**Concept:** A triangular wireframe / lattice that suggests both a stylized capital A and an architectural truss. Evokes the scope-cell lattice from canvas §5 (tuples over BU × env × geo × cloud × data_class × product) and the engineering-precision aesthetic of an infrastructure platform. The A-ness emerges from geometry, not typography.

**Color treatment:**

- Mark color(s): `#bc4808` (the model's chosen burnt-amber tone from `#d97706` prompt seed) on light bg canonical; `#fdba74` orange-300 on dark
- Background neutrality: dual-variant

**Contrast measurement (WCAG 2.2):**

- `mark-1024.png` against `#0a0a0a` (dark): 3.84:1 — FAIL
- `mark-1024.png` against `#fafafa` (light): 4.94:1 — PASS
- `mark-1024-dark.png` against `#0a0a0a` (dark): 11.74:1 — PASS
- `mark-1024-dark.png` against `#fafafa` (light): 1.62:1 — FAIL

**Wordmark provenance:** none — mark-only.

**AI provenance:**

- Model: flux-1.1-pro (Replicate `black-forest-labs/flux-1.1-pro`)
- Model version: pinned by Replicate (latest as of 2026-05-15)
- Generation timestamp: 2026-05-16T01:15Z
- License of output: Flux 1.1 Pro commercial-use-OK per TOS; Apache-2.0 compatible.

**Prompt (verbatim):**

```
Minimalist abstract logo mark. Geometric lattice of thin straight lines forming an interconnected triangular mesh that subtly arranges itself into a stylized capital letter A shape, with negative space inside. The mesh is regular and structured like a crystalline framework or wireframe. Amber orange #d97706 color on pure white background. Flat 2D vector style, no shading, no gradients, sharp clean lines. Centered, generous whitespace. The A shape emerges from the lattice geometry — do not include any rendered typography or alphabet characters. No text, no words, no labels.
```

**Strengths:** Architectural rigor — reads as a real engineered truss. Distinct from any of the other candidates. The wireframe density gives it texture without being busy. Amber is the only warm color in the slate; differentiates strongly.
**Weaknesses:** The A is subtle — viewers may not see the letter at first glance (which can be a strength or weakness depending on the brand goal). The wireframe complexity may not survive favicon-scale (16px) cleanly.
