# Candidate 02 — Control-graph nodes

**Concept:** A central anchor node connected to six radiating nodes — a literal control graph. Directly visualizes the platform's core thesis from canvas §3: one control radiates to many framework satisfactions via STRM-typed edges through SCF anchors.

**Color treatment:**

- Mark color(s): `#115e59` teal-800 (light-bg canonical); `#5eead4` teal-300 (dark-bg variant). Recolored from the model's default teal so the canonical passes WCAG ≥4.5:1 on `#fafafa`.
- Background neutrality: dual-variant

**Contrast measurement (WCAG 2.2):**

- `mark-1024.png` against `#0a0a0a` (dark): 2.61:1 — FAIL
- `mark-1024.png` against `#fafafa` (light): 7.27:1 — PASS
- `mark-1024-dark.png` against `#0a0a0a` (dark): 13.38:1 — PASS
- `mark-1024-dark.png` against `#fafafa` (light): 1.42:1 — FAIL

**Wordmark provenance:** none — mark-only.

**AI provenance:**

- Model: flux-1.1-pro (Replicate `black-forest-labs/flux-1.1-pro`)
- Model version: pinned by Replicate (latest as of 2026-05-15)
- Generation timestamp: 2026-05-16T01:14Z
- License of output: Flux 1.1 Pro commercial-use-OK per TOS; Apache-2.0 compatible.

**Prompt (verbatim):**

```
Minimalist abstract logo mark. Network of 6 small connected circular nodes arranged in a hexagonal-leaning pattern, joined by clean thin straight lines forming a graph. The nodes vary slightly in size, one central larger node anchors the composition. Teal #0d9488 nodes with darker slate #334155 connection lines on pure white background. Flat 2D vector style, geometric precision, no shading, no gradients. Centered, generous whitespace. No text, no letters, no words, no labels. Editorial design quality.
```

**Strengths:** Crystal-clear control-graph metaphor — the exact thing the platform IS. Reads at favicon-scale (16px would still resolve as a node graph). Mark-only is flexible — composes with any wordmark treatment later.
**Weaknesses:** Visually generic "network diagram" silhouette; many tech logos use the same hub-and-spoke convention. Raster only (no SVG).
